# 🎬 Movie Ticket Booking System – Backend (Spring Boot)

This project is a **secure and scalable movie-ticket booking backend** built using **Spring Boot**, featuring robust seat-locking, authentication, and transactional booking workflows.

---

## 🚀 Features

- **Secure Spring Boot backend** for managing movies, shows, seats, and bookings.
- **Synchronized seat-locking mechanism** to prevent race conditions and ensure reliable ticket issuance.
- **JWT-based authentication & authorization** (admin/user roles).
- **Spring Security** integrated for authentication filters, role-based access control, and secure API endpoints.
- **RESTful API design** for all core operations:
  - User management
  - Movie & show management
  - Seat selection and booking
  - Seat-locking orchestration
- **MySQL + Hibernate (JPA)** for relational data modeling and persistence.

---

## 🛠 Tech Stack

- **Spring Boot**
- **Spring Security**
- **JWT Authentication**
- **MySQL**
- **Hibernate (JPA)**
- **Maven**

---

## 📌 Core Backend Highlights

- Built a secure backend for a movie-ticket booking system using **Spring Boot**, including **synchronized seat-locking logic** to prevent race conditions and ensure reliable ticket issuance.
- Implemented **JWT-based role-based authentication** (admin/user) with **Spring Security**, and designed **RESTful APIs** for user management, movies, shows, seats, and ticket booking—backed by **MySQL** and **Hibernate (JPA)** for relational data handling.

---

## 🔮 Future Work

### **Angular UI Frontend**

A full **Angular UI** will be added to complete the system:

- Movie browsing and search
- Real-time seat availability visualization
- Interactive seat selection
- User authentication + dashboard
- Booking history & payment workflow
- Admin interface for movie/show management

This update will turn the project into a complete **full-stack application** with Spring Boot + Angular.

---

## 📁 Project Status

Backend: ✅ Completed  
Angular UI: 🔄 In Progress / Planned  

---

